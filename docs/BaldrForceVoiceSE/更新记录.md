# 更新记录

## v0.1.1-test1

基于 `v0.1.0` 的剧情语音映射增补测试版，运行时代码路径不变。

本轮处理用户增补后的 `最终一次性人工确认_全部.xlsx`：

- 新增用户人工确认通过 17 条；
- 对余下 28 条待定位主线候选改用目标前后各 20 条剧情记录重新核对；
- 19 条通过长上下文唯一重新定位，加入测试映射；
- 9 条仍无法唯一定位，继续排除；
- 相对 v0.1.0 共新增 36 条映射；
- 唯一安全 VoiceMap 从 5855 增至 5891；
- 有映射 BFE 脚本从 244 增至 245；
- 4 条 `ren47c.bin` 多 VoiceTag 冲突继续安全排除；
- 战斗语音研究仍不进入运行时。

新增接档数据：

- `source\data\人工复核输入_20260920.xlsx`
- `source\data\人工增补确认通过.csv`
- `source\data\前后20条定位复核.xlsx`
- `source\data\前后20条定位通过.csv`
- `source\data\前后20条仍未定位.csv`

这些文件用于解释本轮 36 条新增映射的来源，任意单包均可独立复核。

## v0.1.0

第一个正式发行版。

以 `v0.1-test3` 的已验证剧情语音路径为功能基线并进行发行清理：

- 5855 条安全剧情 VoiceMap；
- `Voice3.pac` 原 PAC manager 挂载；
- 普通数字 `@v` 原版路径；
- `TRxxxx` 最小兼容；
- Backlog 原生回放；
- 删除运行日志、覆盖率统计与运行时 CSV；
- 完全移除 test4～test12 的 Battle Voice 实验代码。

## test12（战斗研究版，未进入正式/当前剧情分支）

补回 BFSE 相比 BFE 唯一新增的 category-0 Battle Voice 调用入口后，普通攻击开始自然出现主角语音；证明战斗语音行为移植技术上可行，但尚未完成 BFSE 行为等价审计。

## test11

删除强制试听，仅审计自然 BattleSound 播放入口。实机观察到原版已有少量自然调用。

## test10

35 个 `tooru.dat` 非空槽全部经原 `SoundLoad` 成功载入，35/35 BattleSound 对象建立；主动试听证明资源/对象层闭合，但该试听不是自然 BFSE 行为。

## test4～test9

完成 Battle Voice loader、`tooru.dat`、`Se\Voice`、对象数组与失败路径定位，并修正过错误 CALL-site RVA、错误 loader RVA 以及虚拟文件层假设。

## test3

扩展到 5855 条安全剧情映射；成为 v0.1.0 的剧情核心基线。

## test2

加入 `TRxxxx` 最小兼容；`TR0001`～`TR0007` 与 Backlog 实机通过。

## test1

首次确认 `Voice3.pac` 可挂入原版 PAC manager，普通数字主角剧情 VoiceTag 可走原版语音系统。
