# 更新记录

## v0.1-test1

首个可编译测试版。

- 确认 BFE/BFSE `BGMInfo.DAT` 均为 30×0x44 + 9 字节表尾；
- 确认 +0x00 是 PAC WAV 资源名；
- 确认 +0x40 是 1/100 秒 BGM 循环起点，-1 为非循环；
- 确认 BFE/BFSE 通过相同 BGM 编号索引 30 条表；
- 不修改剧情脚本；
- 不修改 Table.pac；
- 不重打包 BGM.pac；
- BGMInfo 内存只替换 BFSE 循环点，不覆盖 BFE/汉化版资源名字段；
- 两处原 BGM loader CALL 改为运行时按编号传入 BFSE 文件名；
- 支持 `Source=Game`：读取游戏看到的 BGM.pac，兼容 Ultimate ASI Loader update 虚拟覆盖；
- 支持 `Source=Local`：读取 ASI 同目录 `BFSE_BGM.pac`；
- 增加中文诊断日志；
- 提供 BGMInfo 生成器、中文对照 CSV 和 PE 构建验证器；
- 当前等待实机测试。
