# 更新记录

## v1.0.4-crashfix1 — 2026-09-21

- 根据 `evidence\v1.0.3_第一次_八木泽名片崩溃.log`，首次取得真实异常：`EIP=0x0048A974`、读取 `0xFFFFFFFF`，且崩溃前 `glyph_failures=1`。由此把八木泽名片崩溃从“脚本长度猜测”转为**GBK 字形失败后错误回退 CP932**的问题。
- 普通 GBK renderer 继续优先沿用已实机通过的 ANSI/GB2312 路径；`GetGlyphOutlineA` 失败时新增 `CP936 -> Unicode -> GetGlyphOutlineW` 安全回退。
- 一旦字符已经被 Runtime 判定为 GBK，即使 ANSI 和 Unicode 两条 GDI 取字都失败，也返回合法全 0 字形缓冲；不再返回 NULL 让汇编 stub 回原版 CP932 字形路径。
- 低层 1bpp mask 路径同步使用同一 Unicode fallback，避免 Information/TORU 等特殊文字链以后因单个缺字回归。
- 根据 `evidence\v1.0.3_第二次_Backlog崩溃.log`，确认 `jyosyo05.bin #64` 为 JA 20 -> ZH 26、源 count=21；v1.0.3 的运行时 value object 仍保持 count=21，符合“末尾截半个 GBK + Backlog 再消费崩溃”的现场。
- 正式建立**双长度语义**：脚本源游标继续按原日文 `count` 前进；运行时 `dest-4 byte_count` 改为实际中文 GBK 长度+NUL。BFET 与 after.bin direct-entry 都遵循该规则。
- #64 新日志会明确显示：源 count=21，运行时 value object count=27。
- `BaldrForceCN.ini` 恢复，但只暴露 `FontFace` 和 `EnableLog`。其它编码、charset、Hook、BFET、资源等架构参数仍固定，不允许用户关闭。
- `FontFace` 同时影响游戏 `CreateFontIndirectW` Hook 和 Runtime GBK GDI 字形生成；留空保持旧的宋体/黑体规则。
- `EnableLog=0` 时本次启动不创建/覆盖 `BaldrForceCN.log`；默认 1。
- 新增 `docs\INI设置说明.md`；两份 v1.0.3 实机崩溃日志加入 evidence；所有文档同步为本版结论。
- 资源白名单、两个 PAC、10 DAT overlay、9 个差异 loose 资源、三处低层 DBCS routing、两处 glyph-state、两个 1bpp Hook 地址均未改变。

## v1.0.3-crashdiag2 — 2026-09-21

- v1.0.2 保留旧汉化原 `byte_count` 后，用户仍在八木泽名片后同点崩溃；因此“v1.0.1 改写 byte_count 是当前稳定崩溃的唯一直接根因”已被实机证伪。
- 用户新增 Backlog 复现：`jyosyo05.bin #64` 的 `「………什么……」` 末尾出现方块/结束符缺失，随后打开 Backlog 崩溃。静态精确匹配为 JA 20 bytes -> ZH 26 bytes、原 count=21，若二次消费者按原 count 复制会截在 GBK 双字节中间。
- 新增 `audit_bfet_backlog_risks.py`：34,184 条中 642 条中文更长，其中 627 条存在同类半字符截断风险；只有 1 条中文自身 `「/」` 不平衡、6 条中日引号数量变化，因此 #64 的缺失结束符优先判断为运行时截断，而非普遍翻译漏符号。
- 崩溃诊断从 crashdiag1 级别全面扩充：脚本环形缓冲 32 -> 128，字形/DBCS 环形缓冲 64；脚本事件新增文件名、mapping #、state/body/limit/post_cur、value type/count、日/中 hash/长度和前 32 字节原始预览。
- VEH 现在记录全部异常参数、通用/段寄存器、EIP/故障目标内存、EAX~EDI/EBP 指向内存、32 个栈 DWORD、前 64 个栈 DWORD 中代码候选、最多 16 层 EBP frame、运行计数、最近 12 个仍可读 value object 和最近 64 次字形事件。
- 地址归属除主 EXE/CN ASI RVA 外，新增 `VirtualQuery AllocationBase + 模块 RVA`，方便异常落在其它映像时直接定位。
- 诊断仍严格只记录：VEH 永远 `EXCEPTION_CONTINUE_SEARCH`，不改 EIP、不吞异常；日志末尾主动 `FlushFileBuffers`。
- 专门为 #64 增加一次性 `[诊断][Backlog复现句]`，但不修改 count 或 Backlog 逻辑，等待真实异常 EIP/对象现场再修。
- 新增证据截图 `evidence/v1.0.2_Backlog前扩张文本末尾方块_打开Backlog崩溃.png` 和 `data/BFET_Backlog风险审计.*`。
- 资源、字体、DBCS、DAT/WAZ、HELLMODE、两个 PAC、9 个内嵌差异 loose 文件全部冻结不动。

## v1.0.2 — 2026-09-21

- 用户在主线 `jyosyo05.bin` 的“国连军治安维持局信息管理处第一小队队长 / 八木泽 宗次”名片场景后稳定崩溃。移除 Gamepad/BGMSE/VoiceSE，只保留 CN ASI 后仍同点崩溃，因此确认是 BaldrForceCN 自身问题。
- 对旧 46MB 汉化主模块内存映像重新反编译，确认旧汉化等价脚本复制函数约在 `0x004ACB40`：先按**原始 count**推进 `state+0x114`；命中 BFET 映射时只 `strcpy(dest, translated_string)`；没有任何“把调用者 byte_count 改成中文长度”的写入。
- 发现 v1.0.0/v1.0.1 的 `Hook_ScriptCopyBytes()` 多做了 `*(dest-4)=outn+1`，改变了 opcode 0x0B value object 元数据。
- 新增 `tools/audit_bfet_length_semantics.py` 全量审计 34,184 条 BFET：`3909` 条等长、`29633` 条中文更短、`642` 条中文更长，长度不同合计 `30275`。因此旧写法具有广泛潜在崩溃风险。
- 当前复现标题卡本身 `71 -> 71`；紧随其后的 mapping #50 为 `142 -> 108`（含 NUL 时 count `143 -> 109`），与“标题卡已显示、继续时崩溃”的时序高度一致。
- v1.0.2 改为旧汉化等价语义：命中 BFET/direct-entry 时只复制中文与 NUL，**不再修改 `dest-4`**；未命中时才按原始 count 原样复制。
- 新增 `count <= limit-cur` 边界检查，防止损坏状态导致 Hook 自身越过脚本 body 读取。
- 新增复现标题卡一次性 `[诊断]` 日志及“长度不同但保留原 byte_count”的计数日志。
- 9 个差异 loose 资源、2 个 PAC、10 DAT overlay、字体/DBCS/1bpp 链、中文日志与日志路径均保持 v1.0.1 现状，不因本轮崩溃修复扩大改动范围。
- 新增 `docs/脚本复制语义与崩溃调查说明.md`、复现截图与复现日志，确保任意单包可独立接档。

## v1.0.1 — 2026-09-20

- 基于用户已完成并实机满意的 v1.0.0 正式版继续维护；**不修改任何翻译映射、Hook 地址、资源覆盖、字体逻辑、BFET、DAT、WAZ、HELLMODE 或 PAC 内容**。
- `BaldrForceCN.log` 的所有当前运行时人类可读提示统一改为简体中文。等级前缀固定为 `[信息] / [成功] / [警告] / [失败] / [命中] / [追踪] / [诊断]`。
- 技术标识（BFET、GBK、DBCS、IAT、Hook、API 名、文件路径、地址、十六进制值）保留原文，保证逆向和故障定位的精确性。
- 新日志创建后先写 UTF-8 BOM，确保 Windows 记事本等工具能稳定识别中文编码。
- `after.bin` 一次性命中提示同步中文化为 `[命中] after.bin 直接 GBK 脚本文本已执行（运行时已确认）`。
- 版本号升为 `v1.0.1`；v1.0.0 仍保留为功能冻结/实机通过的前一基线，本版属于纯日志本地化维护版。
- 历史 `evidence\测试*_实机日志.txt` 保持原始内容，不为了“中文化”篡改既有测试证据。

## v1.0.0 — 2026-09-20

- `v0.2-test11-dbcslow1` 的 Information/阅览信息与 TORU 练习招式乱码已由实机确认完全修复；该低层文字链正式冻结。
- `v0.2-test12-assets1` 的 HELLMODE 中文图片已由实机确认恢复，证明旧 VFS 图片资产路径方案成立。
- 用户对旧汉化 VFS 除两个 PAC 外的 311 个 loose 文件与 clean 原版进行了全量二进制比较：只有 9 个文件不同，其余 302 个逐字节完全一致。
- 最终版内嵌资源从 test12 的“TOORU.WAZ + 整个 BMP/Hell 16 文件（共 17 个）”收缩为 **9 个真实差异文件**：3 个 `Bmp/Hell`、3 个 `Dat/Cpu`、3 个 `Dat/Waza`。这既保留旧汉化所有实际差异，又删除 13 个与原版完全相同的冗余 Hell 资源。
- 新增用户差异对比截图 `evidence/原版对比_311个loose文件仅9个有差异.png`，并把用户提供的 `recovered_vfs.zip` 固化为 `tools/研究工具/旧汉化差异资源_9文件.zip`。
- `data/旧汉化VFS资源审计.csv` 重做为最终 313 文件发行清单：`11 COVERED = 2 PAC + 9 loose`、`302 PASS`、`0 REVIEW`。
- `after.bin` 保持在 `Update.pac` 中；新增一次性运行日志，当其直接 GBK 脚本文本真正执行时输出 `[HIT] after.bin direct GBK script text active (runtime execution confirmed)`。
- `BaldrForceCN.ini` 继续取消；日志继续固定在 ASI 同目录；release 继续只有 `BaldrForceCN.asi / Update.pac / Chapter.pac`。
- 版本号正式升为 `BaldrForceCN Runtime v1.0.0`。后续定位为维护/查漏补缺版本，除非有明确实机回归证据，不主动重写已通过主线。

## v0.2-test12-assets1 — 2026-09-20

- 用户实机确认 test11 **Information / 阅览信息与 TORU 练习模式招式列表已经完全修复**。因此 test11 的三处低层 DBCS routing、两处 glyph-state 写入和两个 1bpp mask Hook 从本版起视为稳定基线，不再主动改动。
- 复盘发现 HELLMODE / Trial 菜单仍显示日文图片文字；用户同时提供旧 46MB 汉化同位置截图，确认旧汉化确实有中文图片资产。
- 从旧汉化 VFS 恢复包确认 `Bmp\Hell` 目录共 16 个文件、1,986,103 bytes，包括 `MenuMsg.grp`、`Font.grp`、`hell_Menu01.grp` 等；clean/旧运行时反编译也存在对应 `Bmp\Hell\...` 文件路径，因此把这组目录定为 HELLMODE 图片汉化 oracle。
- 把已确认中文化的 `Dat\Waza\TOORU.WAZ` 与完整 `Bmp\Hell` 16 文件全部直接编译进 `BaldrForceCN.asi`，合计 17 个内置 loose 资源。
- 新增 `source\embedded_assets.S`：使用 Clang 集成汇编器 `.incbin` 原字节嵌入资源，不重编码、不重新压缩。
- `CreateFileA` Hook 从“外部 `BaldrForceCN_data\Waza` 目录存在即重定向”改成“只匹配 17 个固定旧汉化路径”。命中后创建隐藏、临时、`FILE_FLAG_DELETE_ON_CLOSE` 的真实 Windows backing file，把 ASI 内嵌字节写入后返回真实文件句柄；其它路径全部直接走原游戏。
- 这种实现不伪造 HANDLE，所以游戏后续 `GetFileSize / ReadFile / SetFilePointer / CloseHandle` 仍由 Windows 正常处理；句柄关闭时临时 backing file 自动删除。
- `BaldrForceCN.ini` 正式取消。字体 Hook、WideCharToMultiByte fallback、GB2312 charset、空强制字体名和 `ContextBytes=128` 全部固定在源码中。
- release 收缩为仅 `BaldrForceCN.asi + Update.pac + Chapter.pac` 三个文件；不再发布 `BaldrForceCN.ini`、`BaldrForceCN_data`、loose TOORU/HELL 资源。
- `source\assets` 保留 17 个旧汉化原始资源，保证源码包离线可复现；这部分不是运行时部署内容。
- 构建流程更新为 C + `embedded_assets.S` 双对象链接；LLVM 路线固定 `-Wall -Wextra -Werror`，并用 `/safeseh:no` 允许纯数据汇编对象参与 Win32 链接。
- 更新 `verify_static.py`：新增 17 个内置资源大小/SHA256、ASI 内每个 payload 精确出现一次、release 仅三文件、INI/loose 数据目录已移除、内置资源逻辑/汇编清单存在等检查。
- 新增 test11 成功截图、test11 完整日志、当前 HELLMODE 日文截图和旧 46MB 汉化 HELLMODE 中文截图到 `evidence`。
- 当前容器重新编译成功；ASI 为 Win32/x86 PE32 DLL，Import Directory 仍为 0。

## v0.2-test11-dbcslow1 — 2026-09-20

- test10 实机确认两个 lookup 同步 Hook 和两个低层 mask Hook 均已成功安装，因此 test9 的“Hook 未安装”问题已经解决。
- test10 的前 12 条 TRACE 给出决定性字节证据：旧汉化正确文本“相马透”的 GBK `CF E0 C2 ED CD B8` 被 clean 低层路径拆成 `CF | E0C2 | ED20 | CD | B8`。这证明错误发生在 lookup 之前的 Shift-JIS DBCS 分组，而不是 GDI mask 之后。
- 重新扫描旧 46MB 汉化运行时所有 `call 0x4155B0` 的 caller，确认只有三个低层文本循环把 clean 的 `cmp al,A0h` 改为 `cmp al,FFh`：`0x004154AD`、`0x00415B2D`、`0x004170BF`。test11 精确复刻这三处 A0→FF。
- 重新比对 lookup 函数内部，确认旧汉化真正保存字符状态的位置是 `0x004155E2`（DBCS WORD）和 `0x0041572D`（single BYTE），而不是 test10 使用的函数入口捕获。test11 改为在这两个旧汉化原位置写 `g_lowlevel_current_code`。
- 五个补丁点实行“一次性全验证”：任何一处 clean 原字节不匹配，则五处都不开始写入；失败日志会直接打印五处真实 bytes。
- 删除 test10 的 lookup-entry trampoline/capture workaround；保留 `0x00415840 / 0x00415CF1` 1bpp mask Hook 与前 12 条 TRACE。
- 新增 test10 两张 `?` 乱码截图及完整实机日志到 `evidence`，把 `CF | E0C2 | ED20 | CD | B8` 固定为本轮上游 tokenizer 证据。
- `verify_static.py` 同步增加五个 clean-2003 原字节检查，并检查 test10 入口捕获方案已从正式源码移除。
- 日志继续跟随 `BaldrForceCN.asi` 同目录；资源层继续冻结为 10 DAT overlay + 单独 `TOORU.WAZ`。
- 容器 Clang/lld 重新构建成功，额外 `-Wall -Wextra -Werror` 编译通过。

## v0.2-test10-glyphsync2 — 2026-09-20

- test9 实机确认了新的关键事实：Information 与 TORU 练习列表从“整页重复同一个 `息`”变成了“每个位置都是不同的乱码”，说明内容字节在逐字推进，且 test9 的低层 mask 回退到了原游戏字形。
- test9 日志同时明确显示 `low-level glyph-code lookup signatures DBCS/single=0/0`，因此 `0x004155B0 / 0x00415710` 两个同步 Hook 实际一个都没有安装；test9 不能算同步方案失败，而是安装阶段根本没有进入。
- 重新核对旧汉化与 clean 2003 反汇编，确认 test9 `0/0` 的根因：test9 误用了旧汉化入口里 `[0x0053400C]` 的绝对操作数；clean 2003 同一函数实际读取 `[0x0053501C]`。test10 已把两个长签名改为 **clean 2003 真值**。
- 同时保留固定 RVA 安全回退：DBCS 入口必须先验证 clean 覆盖字节 `A1 1C 50 53 00 56`，single 入口必须先验证 `A1 1C 50 53 00`；任一不符就安全拒绝安装。
- 保留 `[OK] fixed clean-2003 RVA fallback validated...` 日志：只有 clean 长签名未直接命中而回退校验成功时才出现；正常预期是 clean 真签名直接命中。
- 低层 mask 新增仅前 12 个字符的 `[TRACE] synchronized low-level mask #N code=...`，用于直接证明“当前字符捕获 → mask 消费”是否逐字变化；超过 12 个后停止输出，避免战斗日志膨胀。
- 把 test9 两张新截图和完整日志加入 `evidence`：它们固定证明“资源层命中 + 长签名失败 + 原字形逐字乱码”的中间状态。
- 日志仍始终跟随 `BaldrForceCN.asi` 同目录；正式 release 仍只带确认中文化的 `TOORU.WAZ`。
- `verify_static.py` 同步改为验证 **clean 2003 正确长签名** 的唯一地址，并额外验证固定 RVA 回退会覆盖的完整原指令字节；这样可直接阻止再次把旧汉化 `0x005340xx` 操作数混入 clean 版签名。
- Linux/容器 Clang + lld-link 重新构建成功；输出 Win32/x86 PE32 DLL，Import Directory 为空。

## v0.2-test9-glyphsync1 — 2026-09-20

- 根据 test8-content1 实机日志重新定位 Information / 练习模式的剩余乱码：资源层已经命中，真正错误在低层 1bpp blitter 使用了普通 renderer 留下的陈旧字符代码。
- 重新反编译旧 46MB 汉化运行时，确认 `0x004155B0`（双字节查找）和 `0x00415710`（单字节查找）都会把当前字符写入旧汉化专用状态，随后 `0x00415840 / 0x00415CF1` 的 `.zeas` 低层 mask 路径消费该状态。
- 新增 `g_lowlevel_current_code`，并为 `0x004155B0 / 0x00415710` 增加最小入口 Hook：先捕获 BYTE/WORD 字符，再重放被覆盖的完整原指令并回到 clean 2003 原函数。
- 低层 1bpp mask Hook 改为使用同步后的 `g_lowlevel_current_code`；不再用 `g_renderer_last_code` / `g_renderer_custom_pair` 作为这些特殊界面的字符来源。
- 新日志改为 `[HIT] synchronized low-level 1bpp glyph mask active...`，便于和 test8 的旧路径区分。
- 按用户要求，`BaldrForceCN.log` 从本版开始始终写在 `BaldrForceCN.asi` 同目录；资源与 INI 仍保持原游戏目录布局，避免一次改变两个变量。
- 清理 test8 实际 release 误带的整批 WAZ / `Tama.dat`，本版只保留已经确认中文化的 `TOORU.WAZ`。
- 把用户提供的 test8 故障截图与旧 46MB 汉化正确截图加入 `evidence`，固定 Information / 练习模式视觉 oracle；同时确认旧汉化中的 `戻る` 本来就保持日文，不列入缺失翻译。
- 更新 `verify_static.py`：增加 `glyph.lookup.dbcs @ 0x004155B0`、`glyph.lookup.single @ 0x00415710`、低层同步源码结构、ASI 自身 module-handle 日志路径等检查。
- Linux/容器 Clang + lld-link 重新构建通过；输出为 Win32/x86 PE32 DLL，Import Directory 为空。

## v0.2-test8-content1 — 2026-09-19

- 以实机验证的 gbk7 renderer 为显示基线，不再调整主 glyph 坐标。
- 新增 144 条 clean 2003 EXE ↔ 旧汉化 runtime 对齐后确认的静态中文文本。
- EXE 翻译静态表从 873 条增加到 1017 条；另保留 168 条 CP932→GBK 同字编码归一化。
- 新增干净 2003 资源打开函数 `0x004A9D40` 内联 Hook 与 trampoline。
- 从 localized `Update.pac` 直接 overlay 10 个已确认未压缩 DAT，重点覆盖 Information、Database、Equipment、Item、Flow 等数据。
- 新增 `CreateFileA` WAZ 逐文件重定向机制。
- 本版只随包启用已确认中文化的 `TOORU.WAZ`；未确认的 WAZ 不替换，避免把仍为日文的旧 VFS 文件送进 GBK renderer。
- 静态验证新增：资源 opener 唯一签名、144 条新静态表、10 个 DAT 的大小/SHA256、TOORU.WAZ 精确哈希。
- x86 Clang/lld 构建通过，0 warning；ASI PE32/i386，Import Directory 为空。

## v0.2-test7-gbk7

- 恢复 gbk4 中能够完整显示文字的主 2bpp glyph 路径。
- 补齐旧汉化 `0x00415840` / `0x00415CF1` 两处低层 1bpp glyph-mask Hook。
- 实机确认基本恢复到此前完成度最高的中文字体效果，仅剩个别缺字与内容来源问题。

## v0.2-test6-gbk6

- 尝试去除 `tmAscent` 的纵向公式，实机失败：文字仍只剩横线/碎片。

## v0.2-test5-gbk5

- 加入 168 条 CP932→GBK 同字编码归一化。
- glyph 行方向修改错误，实机导致文字大面积消失。

## v0.2-test4-gbk4

- 恢复旧汉化全局 `0x80..0xFE` 双字节消费规则。
- 实机大面积乱码消失，证明字节消费主线正确，但仍存在低层 glyph 图层问题。

## 更早阶段

- BFET 300 文件 / 34,184 条映射已恢复；脚本 opcode `0x0B` Hook 已实机命中。
- 211 条 EXE 硬编码 UI/System 文本已实机验证。
- MoleBox/STEELBOX VFS 已完整恢复 313 文件，并证明“直接把 313 loose 文件覆盖本体”不能等价恢复旧汉化。
